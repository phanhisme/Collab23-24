using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayerStatAnim : MonoBehaviour
{
    //public Animator animator; // Reference to the Animator component of the UI element
    //public Button button; // Reference to the Button component

    //void Start()
    //{
    //    // Get the Animator component attached to the UI element
    //    animator = GetComponent<Animator>();

    //    // Get the Button component attached to the button GameObject
    //    button = GetComponent<Button>();

    //    // Add listener to the button's onClick event
    //    button.onClick.AddListener(PlayAnimation);
    //}

    //void PlayAnimation()
    //{
    //    // Trigger the "PlayAnimation" parameter in the animator
    //    animator.SetTrigger("PlayAnimation");
    //}
}
